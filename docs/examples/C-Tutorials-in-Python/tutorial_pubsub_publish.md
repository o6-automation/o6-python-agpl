# Working with Publish/Subscribe - Publishing Fields

Source: `examples/highlevel/c-tutorial-examples/tutorial_pubsub_publish.py`

The open62541 tutorial configures a PublishedDataSet, WriterGroup, and
DataSetWriter. The high-level Python API in this repository does not currently
expose PubSub setup helpers, so this page is a placeholder for the missing API
surface.

```python
def main() -> None:
	print("PubSub publish parity is not yet available in high-level o6 Python API.")


if __name__ == "__main__":
	main()
```
