# Subscribing Fields

Source: `examples/highlevel/c-tutorial-examples/tutorial_pubsub_subscribe.py`

The open62541 tutorial configures a PubSub subscriber, ReaderGroup, and
DataSetReader. The high-level Python API in this repository does not currently
expose PubSub subscription helpers, so this page remains a placeholder until
that surface lands.

```python
def main() -> None:
	print("PubSub subscribe parity is not yet available in high-level o6 Python API.")


if __name__ == "__main__":
	main()
```
